name: Release

on:
    workflow_run:
        workflows: ["Build"]
        types:
            - completed
    push:
        tags:
            - "v*"

jobs:
    release:
        strategy:
            matrix:
                os: [ubuntu-latest, windows-latest]
        runs-on: ${{ matrix.os }}
        steps:
